# Agent Zero v6.0 - API Configuration
# ==========================================

# Builder API Configuration (用于构建 Agent 时的推理)
# 推荐使用强模型以获得更好的代码生成质量
BUILDER_PROVIDER=openai
BUILDER_MODEL=deepseek-chat
BUILDER_API_KEY=sk-a1b7bce852e340dd8bb38aa1e6a67dd8
BUILDER_BASE_URL=https://api.deepseek.com
BUILDER_TIMEOUT=60
BUILDER_MAX_RETRIES=3
BUILDER_TEMPERATURE=0.7

# Runtime API Configuration (用于生成的 Agent 运行时)
# 可以使用相同或更经济的模型
RUNTIME_PROVIDER=openai
RUNTIME_MODEL=deepseek-chat
RUNTIME_API_KEY=sk-a1b7bce852e340dd8bb38aa1e6a67dd8
RUNTIME_BASE_URL=https://api.deepseek.com
RUNTIME_TIMEOUT=30
RUNTIME_TEMPERATURE=0.7

# Embedding API Configuration (用于 RAG 向量化)
# 使用本地 Ollama 的嵌入模型，免费且快速
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_BASE_URL=http://localhost:11434
# EMBEDDING_API_KEY 不需要（Ollama 本地运行）