# Phase 6 测试指南

## 方式一: 使用测试脚本 (推荐快速验证)

### 1. 设置 API Key

编辑 `test_phase6_e2e.py`:
```python
builder_api_key="sk-your-actual-api-key-here"
```

### 2. 运行测试

```bash
python test_phase6_e2e.py
```

选择选项 1 进行完整测试。

### 3. 观察输出

测试会:
- ✅ 创建 Agent
- ✅ 运行测试并生成报告
- ✅ 在每次迭代后询问你是否继续
- ✅ 显示测试结果和错误分析
- ✅ 自动提交到 Git
- ✅ 显示最终进化总结

---

## 方式二: 使用 start.py (真实场景)

### 1. 确保配置正确

检查 `.env` 文件:
```bash
BUILDER_PROVIDER=deepseek
BUILDER_MODEL=deepseek-chat
BUILDER_API_KEY=sk-your-key-here
BUILDER_BASE_URL=https://api.deepseek.com
```

### 2. 运行 start.py

```bash
python start.py
```

### 3. 输入需求

```
请描述你想创建的 Agent:
> 创建一个文档问答 Agent，回答关于 Python 的问题
```

### 4. 交互流程

系统会:

**步骤 1: 需求分析**
```
🚀 [步骤 1/6] 需求分析...
✅ 需求分析 完成。
```

**步骤 2-5: 设计、仿真、编译、测试**
```
...
```

**步骤 6: 迭代优化 (Phase 6 新功能!)**

每次测试后会显示:
```
======================================================================
📊 迭代 0 总结
======================================================================

🧪 测试结果:
   - 总测试数: 6
   - 通过: 2 ✅
   - 失败: 4 ❌
   - 通过率: 33.3%

🔍 错误分析:
     - rag_quality: 3

🎯 修复目标: rag_builder

💡 Judge反馈:
   RAG检索质量不足...

📦 Git提交: abc12345
   消息: Iteration 0: Pass rate 33.3%

======================================================================
命令:
  [y] 继续迭代优化
  [n] 停止迭代
  [feedback] 提供反馈意见后继续

是否继续迭代优化? (y/n/feedback): 
```

**你的选择:**
- 输入 `y` - 继续优化
- 输入 `n` - 停止迭代
- 输入 `feedback` - 提供反馈后继续

**自动停止:**
如果通过率达到 90%，系统会自动停止:
```
✅ 测试通过率达标 (≥90%), 停止迭代
```

**最终总结:**
```
======================================================================
📈 Agent 进化总结: YourAgent
======================================================================

📊 总体统计:
   - 总迭代次数: 3
   - 初始通过率: 33.3%
   - 最终通过率: 100.0%
   - 改进幅度: +66.7%

📉 通过率趋势:
   迭代 0: ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░ 33.3%
   迭代 1: ████████████████████████░░░░░░░░░░░░░░░░ 66.7%
   迭代 2: ████████████████████████████████████████ 100.0%

======================================================================
```

---

## 查看生成的报告

### 报告目录结构

```
agents/YourAgent/
└── .reports/
    ├── iteration_0_20260116_174500.json
    ├── iteration_1_20260116_174800.json
    ├── iteration_2_20260116_175100.json
    └── history.json
```

### 查看报告内容

```bash
# 查看某次迭代的详细报告
cat agents/YourAgent/.reports/iteration_0_*.json

# 查看完整历史
cat agents/YourAgent/.reports/history.json
```

### 使用 Python 读取报告

```python
from src.core.report_manager import ReportManager
from pathlib import Path

# 加载报告
manager = ReportManager(Path("agents/YourAgent"))

# 查看某次迭代
report = manager.load_iteration_report(0)
print(f"通过率: {report.pass_rate:.1%}")

# 查看完整历史
history = manager.load_history()
print(f"总迭代: {len(history.iterations)}")

# 生成总结
summary = manager.generate_summary(0)
print(summary)

# 进化总结
evolution = manager.generate_evolution_summary()
print(evolution)
```

---

## 查看 Git 历史

```bash
cd agents/YourAgent

# 查看提交历史
git log --oneline

# 输出示例:
# abc1234 Iteration 2: Pass rate 100.0%
# def5678 Iteration 1: Pass rate 66.7%
# 9ab0cde Iteration 0: Pass rate 33.3%
# 1234567 Initial generation
```

---

## 故障排除

### 问题 1: 没有看到用户确认提示

**原因**: `interactive` 配置未启用

**解决**:
```python
# 在 AgentFactoryConfig 中设置
config = AgentFactoryConfig(
    interactive=True  # 确保为 True
)
```

### 问题 2: 报告目录不存在

**原因**: Agent 创建失败或跳过了测试

**解决**:
- 确保在用户确认时选择 `y` 安装依赖
- 检查测试是否成功运行

### 问题 3: Git 提交失败

**原因**: Git 未初始化或配置错误

**解决**:
```bash
# 手动初始化 Git
cd agents/YourAgent
git init
git config user.name "Your Name"
git config user.email "your@email.com"
```

---

## 预期结果

成功运行后，你应该看到:

1. ✅ `.reports/` 目录包含所有迭代报告
2. ✅ Git 历史包含每次迭代的提交
3. ✅ 每次迭代后都有用户确认提示
4. ✅ 通过率达到 90% 时自动停止
5. ✅ 最终显示完整的进化总结

---

## 下一步

如果测试成功，你可以:

1. **继续 Phase 2**: 添加 LLM 智能分析
2. **继续 Phase 3**: 添加自动配置优化
3. **投入使用**: 当前功能已足够生产使用!

祝测试顺利! 🚀
